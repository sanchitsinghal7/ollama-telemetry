# Changelog

## 0.1.2

- Accept human-friendly CLI lookback intervals such as `30m`, `24h`, `7d`, and `2w`.
- Retain backwards-compatible whole-number day values such as `--last 7`.
- Add CLI duration parsing tests.

# Changelog

## 0.1.1

- `telemetry.agent(...)` now works both as a decorator and a context manager.
- Corrected public GitHub project URLs.
- Added coverage for both supported agent-trace styles.

## 0.1.0

- Initial TestPyPI release.
