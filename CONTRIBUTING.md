# Contributing

Keep the public package generic and privacy-preserving:

- Do not add organization-specific workflow names, identifiers, paths, schemas, or integrations.
- Do not add prompt, completion, message, raw-payload, or arbitrary-object capture by default.
- Keep base dependencies in the Python standard library.
- Add tests for every new integration and failure mode.
